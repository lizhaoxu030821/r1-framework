# 2026-08-24 ZKY BeyondMimic 起身训练记录

## v11 奖励设计（待服务器验证）

- 保留 ZKY NPZ 逐帧跟踪、frame-0 精确初始化、参考关节位置残差动作。
- 参考 Atom01-Getup-Mimic 和 UFO-RPO 的起身设计，新增：
  - `motion_anchor_height`：显式跟踪参考根部高度；
  - `motion_anchor_upright`：显式跟踪参考根部直立度；
  - `getup_foot_support`：奖励左右踝部接触支撑；
  - `getup_stable_stand`：仅在参考动作结束后，根部高度、直立度、线速度和角速度同时达标时给终端奖励。
- 将根部终止阈值恢复为位置 `0.30 m`、姿态 `0.80`，保留前 50 步保护，防止躺着长期 timeout 形成假成功。

## 当前验证

- Python 语法编译通过。
- 本机不存在 `/data/lzx/conda_envs/robo_lzx`，尚未完成 IsaacLab 配置实例化；需在 `119.6.246.139:22222` 上验证。
- 尚未启动 v11 训练。

## v11 smoke test

- IsaacLab 场景、ZKY 19 DoF、NPZ MotionCommand 和 18 个奖励项均已成功构建。
- 第一个仿真步发现批量四元数奖励的重力向量维度错误，已修正为按环境数扩展；正式训练尚未启动。

## v11 正式训练

- 已通过第二次 smoke test：8 环境、1 iteration，PPO 更新完成，新增奖励均为有限值。
- 已在服务器后台启动：`ZKY-Getup-BeyondMimic-v0`、`4096` 环境、`cuda:1`、`30000` iterations。
- W&B 项目：`zky_getup_beyondmimic`。
- run name：`npz_sequence_height_upright_support_v11_20260824`。
- 启动日志：`logs/launch/npz_sequence_height_upright_support_v11_20260824.log`。
- 服务器进程 PID：`3047783`（启动 shell PID `3047776`）。

## v11 W&B 诊断与物理高度指标

- W&B run `8fck1g4w` 在约 step 1500 时平台：`anchor_pos=1.0`、`time_out=0`、平均 episode 约 147 步，`error_anchor_pos≈0.36 m`、`error_joint_pos≈5.6 rad`，尚未到达完整起身末帧。
- 已停止该明显早期终止的平台训练，未将其视为成功。
- 新增 W&B command metrics：`Metrics/motion/robot_base_height`、`Metrics/motion/reference_base_height`、`Metrics/motion/robot_upright`，用于直接观察真实根部高度、参考高度和直立度。
- 为避免相同平台再次出现，ZKY 终止改为阶段化：参考序列最后 9 帧才启用严格根部位置/姿态误差终止，前面完整暴露起身接触序列。

## RoboParty 结构对齐修正

- 对比 RoboParty/Atom01 成功起身实现后确认：其训练阶段关闭 motion/低高度/重力终止，完整暴露动作，并使用实际躯干高度进度、实际直立度进度、脚接触、速度和稳定持续时间奖励。
- v11 的直接失败原因是 `anchor_pos=1.0`，episode 平均约 147 步，229 帧动作从未完整执行；`error_joint_pos` 约 `5.6 rad`。
- 新增 `getup_height_progress` 和 `getup_upright_progress`，改为奖励机器人实际状态的绝对起身进度。
- 关闭 ZKY 训练阶段 `anchor_pos/anchor_ori` 参考误差终止，只保留 timeout 和物理成功率评估，避免策略在首次抬升阶段被截断。
- v12 smoke test 首次发现高度指标缺少 `quat_apply_inverse` 导入，已修正；终止/奖励结构已成功完成配置构建。
- 第二次 v12 smoke test 已完成 2 次真实 PPO 更新，无 NaN；终止仅剩 timeout，真实高度/直立度指标正常输出。
- 关闭 `randomize_joint_default_pos`，使 nominal frame-0 初始化与 P 控制中心严格一致，匹配 RoboParty 的 nominal 起身训练设置。

## v12 正式训练

- 已启动 `npz_robo_party_progress_v12_20260824`：4096 环境、cuda:1、30000 iterations、W&B `zky_getup_beyondmimic`。
- 服务器训练 Python PID：`3182169`；启动日志：`logs/launch/npz_robo_party_progress_v12_20260824.log`。

## 训练环境

- SSH：`lzx@119.6.246.139:22222`
- Conda：`/data/lzx/conda_envs/robo_lzx`
- GPU：`cuda:1`
- W&B 项目：`zky_getup_beyondmimic`
## 2026-08-24 grounded reference revision

- Compared the failed v12 behavior with the RoboParty get-up structure. The remaining blocker was not only reward shaping: the original ZKY reference places collision geometry as much as 0.376 m below the MuJoCo floor in the middle of the clip.
- Added `retarget/robot_retargeter/scripts/ground_zky_beyondmimic_npz.py`. It reads the existing 19-DoF BeyondMimic NPZ, uses the ZKY fixed MJCF collision geometry, shifts each frame upward to a 5 mm floor clearance, then recomputes body poses and velocities.
- Generated `ZKY_SDK_19Dof/resources/motion/zky/npz/r1_standup_reference_full26_50hz_zky_grounded_full19.npz` (229 frames, finite values). Root height changes from 0.0829 to 0.1612 m at frame 0 and from 0.7278 to 0.9571 m at the final frame; maximum per-frame root-z change is 0.0397 m.
- Switched `zky_getup_beyondmimic_env_cfg.py` to the grounded NPZ. No long training has been started; static audit and smoke test are still required.
- The grounded smoke test loaded successfully on server GPU `cuda:1` (8 envs, 2 iterations, W&B run `qfa140a1`), with finite rewards and the expected 19-D action/101-D policy observation. Because the user's original NPZ replays well in MuJoCo, it remains the training reference; the grounded file is retained for audit only.
- Training-side changes based on RoboParty stable get-up runs: ZKY action scale set to `0.15` (scoped in the ZKY task), PPO initial action noise reduced to `0.30`, entropy coefficient to `0.001`, and learning rate to `3e-4`. These reduce destructive early residual exploration while preserving residual PD tracking.
- Started formal run on `119.6.246.139:22222`, GPU `cuda:1`, 4096 environments, 30000 iterations: `raw_lowexplore_robo_party_v14_20260824` (W&B run is created by the launcher). At early iteration 33, episodes reach the full 300-step timeout with no stable-stand reward yet; robot height is about 0.118 m versus 0.728 m reference and uprightness about -0.145. This is an early diagnostic, not a success claim.
- Stopped v14 at approximately iteration 8898 after convergence to a non-rising policy: height about 0.149 m, upright about 0.063, stable-stand 0, timeout 1.0.
- Added temporal `robot_height_delta` and `robot_upright_delta` metrics with reset-safe previous-state buffers, plus `getup_height_delta` and `getup_upright_delta` rewards. Absolute progress weights were reduced so a static fallen pose cannot keep receiving the same progress incentive.
- User requested reducing parallelism; future formal runs use 512 environments. First delta-reward smoke test exposed and fixed a batched quaternion reset shape bug. Corrected smoke test `delta_reward_smoke_v15b_20260824` passed two PPO iterations with finite delta metrics.
- Preflight `delta_progress_512_preflight_v15_20260824` completed 5 iterations with 512 environments: no NaN/runtime errors, delta metrics remained finite and small. Formal launch is now cleared with 512 environments.
- Per the user's request for a final pre-training review, stopped the just-started formal v15 process before meaningful training. Reviewed IsaacLab step ordering: rewards use the previous valid delta, reset buffers are initialized to the frame-0 root state, and command metrics are updated after reward computation for the next step. No remaining shape/NaN issue was found after the fix; formal launch is currently paused pending review.
- After the review, restarted the cleared formal run with exactly 512 environments on `cuda:1`: `delta_progress_512_v15_20260824`, W&B run `hnbqpr3p`. Removed one stale duplicate process immediately after startup; only the new instance remains.

## 2026-08-24 v16 grounded preflight

## 2026-08-24 RoboParty BeyondMimic architecture audit

- Compared the ZKY task against RoboParty/RPO BeyondMimic. Both use the same deployable actor inputs (reference joint command, base angular velocity, projected gravity, joint position/velocity, and previous action) and privileged critic inputs (reference anchor pose plus simulated body state).
- `motion_anchor_pos_b` and `motion_anchor_ori_b` remain critic-only. They require reference phase and simulator/reference root state and are not direct onboard measurements; adding them to the actor would create a train/deploy mismatch.
- RoboParty's get-up-specific recovery terms are zero-weight in the reference configuration. The transferable mechanism is the dense whole-body tracking/contact regularization and full episode exposure, which ZKY already has.
- No long training was started from this audit. First gate is configuration construction and smoke rollout; success must still be demonstrated in MuJoCo with repeatable height/upright/contact behavior.
- Added a low-weight contact-conditioned `feet_slide` penalty, adapted from RoboParty, to discourage lateral foot skating while preserving prone-phase repositioning.
- v22 ordered curriculum: resets now always start at NPZ frame 0, and stable-stand reward requires `motion_ended`; this removes v21's late-phase teleport reward loophole.
- Fixed frame-zero sampling to bypass zero-mass CUDA multinomial; smoke v22 must be rerun before formal launch.

- v15 was stopped at approximately iteration 1700: height stayed near `0.12 m`, uprightness remained negative, and `getup_stable_stand` stayed zero. This is the same fallen-pose plateau as v14, so v15 will not be extended.
- The next candidate uses the previously generated `r1_standup_reference_full26_50hz_zky_grounded_full19.npz`, whose collision geometry was audited against the ZKY fixed-all model. It is played at `0.5x` and the episode is extended to `10 s` so contact recovery has time to execute.
- ZKY residual action scale is increased to `0.25`; PPO initial noise is `0.40`. A phase-gated non-foot-contact penalty is added after reference height exceeds `0.25 m`, while initial prone contacts remain allowed.
- Added command metrics `robot_min_body_height` and `reference_min_body_height` for the mandatory physics preflight. No long training is allowed until the grounded reference loads, all values are finite, body heights remain above the plane within contact tolerance, and zero-action/short PPO smoke has no NaN, QACC, or runtime failure.
- Grounded preflight passed on server `cuda:1`: 8 env / 2 PPO iterations built and updated successfully; a 20-iteration rollout covering the full `0.5x` clip completed with finite rewards and no NaN/QACC/runtime errors. The minimum simulated body height stayed positive (`0.0175 m` at the lowest sampled aggregate), while the grounded reference minimum stayed positive (`0.0197--0.1403 m` depending on phase), so the scene loads without observed penetration or suspension.
- Formal v16 launched after the physics gate with 512 environments on `cuda:1`, W&B project `zky_getup_beyondmimic`, run name `grounded_contact_phase_v16_20260824`; log `logs/launch/grounded_contact_phase_v16_20260824.log`. At iteration 18 the process is healthy, but the policy is still early and not yet a success: height is about `0.121 m`, uprightness about `-0.10`, stable-stand `0`, and minimum body height remains positive (`0.033 m`).
- v16 was stopped at iteration ~111 after confirming the same fallen timeout plateau (`height ~0.119 m`, `upright ~-0.15`, stable-stand `0`). Audit found phase sampling was disabled and, more importantly, sampled joints would have been paired with a frame-0 root if enabled. The reset now uses the same sampled frame for root pose, root velocity, and joints; v17 enables adaptive phase sampling with `adaptive_uniform_ratio=0.20` and `adaptive_alpha=0.01`.
- v17 phase-reset smoke passed with 16 environments and 5 PPO iterations. Sampled phases produced consistent reference/robot root states, finite rewards, and no NaN/QACC/runtime errors. The body-center diagnostic can be slightly below the floor because it is not a collision-surface measurement; the grounded geometry preflight remains the authoritative penetration check.
- Formal v17 launched as `phase_curriculum_v17_20260824` with 512 environments on `cuda:1`, log `logs/launch/phase_curriculum_v17_20260824.log`. Initial iterations now sample nonzero phases: iteration 6 reports robot height `0.266 m` and uprightness `0.516` against a reference height `0.750 m`, instead of v16's frame-0-only `0.119 m/-0.15` plateau. Stable-stand is still zero; this is an early learning signal, not final success.
- MuJoCo diagnosis of v16 `model_0.pt` found a replay mapping bug, not proof that the NPZ is invalid: the player added the static default pose to actions, while training uses `use_reference_joint_offset=True` and adds the current NPZ joint frame. The player now targets `motion["joint_pos"][frame] + 0.25 * action`. A 300-frame headless replay completed without NaN/QACC; the random model still does not perform a meaningful get-up, as expected for `model_0`.
- v17 was stopped near iteration 2163 after partial height/upright improvements but `getup_stable_stand` remained exactly zero. The stable-stand term now gates on the sampled reference entering the stand-height phase (`reference anchor height >= 0.60 m`) instead of waiting for `motion_ended`, so adaptive late-phase episodes receive a learnable standing signal.
- Bottom-layer audit found the actor observation lacked explicit root target position/orientation; only the privileged critic had these terms. With adaptive phase sampling, joint targets alone are phase-ambiguous. Added `motion_anchor_pos_b` and `motion_anchor_ori_b` to the policy observation; a fresh experiment is required because observation dimension changes.
- v19 actor-root-observation smoke passed (16 env, 5 PPO iterations). Formal run `actor_root_obs_v19_20260824` launched on `cuda:1` with 512 envs. At its early iteration 4, stable-stand reward is `0.8572`, robot height `0.4405 m`, uprightness `0.6005`, and minimum body height `0.0436 m`; this is an encouraging phase-sampled signal but not yet full-action success.
- Review correction: `motion_anchor_pos_b` and `motion_anchor_ori_b` depend on the stored reference phase and target root pose; they are not directly measurable onboard. The actor-observation change was reverted to preserve train/deploy observability parity, and v19 was stopped before evaluation.
- v22 plateau diagnosis near iteration 1000: base height stayed below 0.2 m and timeout was 1.0. Non-foot contact gating was changed from reference height to measured robot height to remove a constant fallen-state penalty.
- v24 stage-1 curriculum limits the ordered reference to frame 40 with 2 s episodes; this isolates the initial contact/support segment before later window expansion.
- v25 stabilizes stage-1 continuation from model_800: PPO learning rate `1e-4`, initial action noise `0.20`, frame-40 window unchanged.
- v26 aggressive stage-1 guidance: action scale `0.30`, initial noise `0.30`, height/upright tracking weights `4.0/2.5`, and temporal progress weights `6.0/4.0`.
- v27 narrows the curriculum boundary to frame 20 and episode length to 1.5 s after v26 plateaued at 0.193 m on the frame-40 target.
- Restored frame-40/2 s curriculum per user request while retaining v26 aggressive guidance.

## 2026-08-24 Reference consistency correction

- Audited the requested `ik_hand_visual.npz`: it contains 12 joints and cannot directly drive the 19-DoF task.
- Compared the 19-DoF source and grounded files. The source root height at frame 40 is `0.1715 m`, while grounded is `0.2548 m`; the policy plateau was `0.170~0.171 m`.
- Switched training back to `r1_standup_reference_full26_50hz_zky_ik_hand_limited_full19.npz`, which preserves the 19-DoF mapping and keeps root/body kinematics consistent with the visual source.
- Source19 smoke passed with finite values and no runtime/shape errors; its reference minimum body height was about `-0.015 m`. Generated `r1_standup_reference_full26_50hz_zky_source_clearance_full19.npz` by applying a uniform `+0.02 m` lift to body/root positions only, preserving all 19 joint trajectories and relative geometry.
- Training configuration now points to the source-clearance file.

## 2026-08-24 MuJoCo deployment alignment audit

- Stopped stale remote v22 smoke and v26 processes.
- Matched MuJoCo residual action scale to training (`0.30`).
- Matched MuJoCo effort limits to ZKY IsaacLab actuators (legs/waist `120 Nm`, ankles/arms `27 Nm`).
